const selectedEl = listRef.querySelector('[aria-selected="true"]'); // flag
const activeEl = document.activeElement; // flag
let rowEl = null; // flag
let el = null; // flag

let parentNode = null;
const menuRef = null;
let previouslyFocused = null;
