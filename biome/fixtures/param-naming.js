function processConfig(cfg) { // flag
  return cfg;
}

function handleEvent(evt) { // flag
  return evt;
}

function debounce(fn, delay) { // flag
  return fn;
}

items.map((el) => el.textContent); // flag

function focusNode(node) {
  return node;
}

function applyOptions(options) {
  return options;
}

items.map((item) => item.id);

function computeIndex(index) {
  return index;
}
