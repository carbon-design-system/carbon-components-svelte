// @ts-check

/**
 * Default window width (px) at which the UI Shell expands its side nav
 * and hides the hamburger menu. Matches the Carbon `lg` grid
 * breakpoint.
 */
export const EXPANSION_BREAKPOINT = 1056;
