// @ts-check

/** Default delay (ms) before a hover/focus tooltip opens. */
export const TOOLTIP_ENTER_DELAY_MS = 100;

/** Default delay (ms) before a hover/focus tooltip closes. */
export const TOOLTIP_LEAVE_DELAY_MS = 300;
