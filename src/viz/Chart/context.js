// @ts-check
// Context shared by `Chart` and the marks composed inside it.

/** @typedef {import("./context.d.ts").ChartContext<any>} ChartContext */

export const CHART_CONTEXT = "carbon:viz:Chart";
