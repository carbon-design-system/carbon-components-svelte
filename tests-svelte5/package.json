{
  "name": "carbon-components-svelte-tests-svelte5",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:types": "svelte-check --workspace ../tests --fail-on-warnings && svelte-check --workspace ./svelte5 --fail-on-warnings"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "svelte": "^5.47.1"
  }
}
