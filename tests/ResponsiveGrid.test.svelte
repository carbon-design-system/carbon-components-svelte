<script lang="ts">
  import { Grid, Row, Column } from "../types";
</script>

<Grid>
  <Row>
    <Column
      sm="{1}"
      md="{4}"
      lg="{8}"
      style="outline: 1px solid var(--cds-interactive-04)"
    >
      sm: 1, md: 4, lg: 8
    </Column>
    <Column
      sm="{1}"
      md="{2}"
      lg="{2}"
      style="outline: 1px solid var(--cds-interactive-04)"
    >
      sm: 1, md: 2, lg: 2
    </Column>
    <Column
      sm="{1}"
      md="{1}"
      lg="{1}"
      style="outline: 1px solid var(--cds-interactive-04)"
    >
      sm: 1, md: 1, lg: 1
    </Column>
    <Column
      sm="{1}"
      md="{1}"
      lg="{1}"
      style="outline: 1px solid var(--cds-interactive-04)"
    >
      sm: 1, md: 1, lg: 1
    </Column>
  </Row>
</Grid>
